# Generative MIDI Sequencer - ユーザー・スタートアップガイド

本ガイドでは、Raspberry Pi Pico (RP2040) をベースとした4チャンネル・ジェネレーティブMIDIシーケンサーの導入手順、ハードウェア接続、画面UIのパラメータ操作、およびパフォーマンス機能について詳しく解説します。

本シーケンサーは、ユーロラック・モジュラーやハードウェア・シンセサイザーと同期し、**ユークリッド・リズム**と**Turing Machine型ランダム旋律生成**を組み合わせた有機的なテクノトラックをリアルタイムに生成・変異させる強力なライブ・パフォーマンスツールです。

---

## 1. クイック・スタート（ファームウェア書き込み）

本ファームウェアは、標準的な **`.uf2`** 形式バイナリに自動パッケージ化されています。特別な書き込みツール（OpenOCD等）を使わず、PCとUSBケーブルだけで即座に導入できます。

1.  **BOOTSELモードで起動**: Raspberry Pi Pico上の白い「BOOTSEL」ボタンを押しながら、PCとUSBケーブルで接続します。
2.  **ドラッグ＆ドロップ**: PC上に `RPI-RP2` という名称のポータブルドライブとして認識されます。
3.  **書き込み完了**: ビルドして生成された `generative_midi_sequencer.uf2` をこのドライブのルートにドラッグ＆ドロップします。書き込みが完了すると、自動的にPicoが再起動してシーケンサーが起動します。

---

## 2. ハードウェア接続ピンアウト (Pinout Specification)

シーケンサーをフル機能で使用するための、PicoのGPIO物理ピンアサインです。ブレッドボードやカスタム基板の結線時にご参照ください。

### 2.1. ディスプレイ接続 (ILI9341 3.2" TFT LCD)

SPI0バスを使用し、超高速48MHz転送で滑らかな60fps UI描画を実現します。

- **VCC** ➔ 3.3V (GP36 / 3.3V OUT)
- **GND** ➔ GND
- **CS (Chip Select)** ➔ **GP17** (Pin 22)
- **RESET** ➔ **GP16** (Pin 21)
- **D/C (Data/Command)** ➔ **GP20** (Pin 26)
- **SDI (MOSI)** ➔ **GP19** (Pin 25)
- **SCK** ➔ **GP18** (Pin 24)
- **LED (バックライト)** ➔ 3.3V（必要に応じて抵抗を介して接続）

### 2.2. MIDIおよびクロック同期出力

- **TRS-MIDI OUT** ➔ **GP0 (TX)** (Pin 1) ➔ 220Ωの抵抗を介してTRS-MIDIジャックのTipへ接続
- **アナログクロック同期 OUT** ➔ **GP15 (I2S data)** ➔ モジュラーシンセのClock/Run入力へ接続 (+3.3V ゲートパルス出力)

### 2.3. 操作キーマトリクス（9キースイッチ）

すべてのスイッチは内蔵プルアップ抵抗で処理されているため、スイッチの片側を以下のGPIOに、もう片側を **GND** に直接結線します（アクティブLOW）。

- **UP (上)** ➔ **GP5** (Pin 7)
- **DOWN (下)** ➔ **GP3** (Pin 5)
- **LEFT (左)** ➔ **GP2** (Pin 4)
- **RIGHT (右)** ➔ **GP4** (Pin 6)
- **PLAY (再生/停止)** ➔ **GP10** (Pin 14)
- **LT (シフト/修飾キー)** ➔ **GP9** (Pin 12)
- **RT (ページ切り替え)** ➔ **GP6** (Pin 9)
- **A (増加 / INC)** ➔ **GP8** (Pin 11)
- **B (減少 / DEC)** ➔ **GP7** (Pin 10)

---

## 3. UI画面構成と操作方法

画面は上部のグローバル情報（BPM、ステータス）と、4行のトラックグリッドで構成されています。

```
BPM:125  [▶ RUN]   [SCL:CHROM]   [💾]
---------------------------------------
TR1  x1  16  08  00  20  00  60  60 S1 RN
TR2  x2  12  04  02  40  20  80  60 S1 RN
TR3  x4  08  03  00  00  00  70  60 S1 RN
TR4  x1  32  12  05  10  50  60  60 S1 RN
```

### 3.1. 基本操作ショートカット

- **カーソル移動**: 十字キー（`UP` / `DOWN` / `LEFT` / `RIGHT`）を押すことで、グリッド上の任意のパラメータセルを選択します。選択されたセルの四隅にはネオンシアンの **コーナーマーカー** が常時表示されます。
- **キー操作**:
  - さらに、**`LT`（Shift）キーを押しながら `A` または `B`** を押すと、パラメータ値が **10ステップずつ高速にスキップ** します。
  - `A` と `B` を同時押しすると、選択中のパラメータに応じた特別コマンドが実行されます。
    - `🧬 Mutation` 選択中：値が即時 `0` にジャンプし、フレーズ固定状態になります。
    - `⁙ Density` 選択中：現在の `Length` の半分に `Density` を設定します（奇数のときは四捨五入相当）。
- **再生/停止**: **`PLAY` ボタン** でシーケンサー全体のStart/Stopを切り替えます。
- **BPM (テンポ) の変更**:
  - **`LT` (Shift) + `UP` (上)**: マスターBPMを **`+1`** 増加させます（上限250 BPM）。
  - **`LT` (Shift) + `DOWN` (下)**: マスターBPMを **`-1`** 減少させます（下限40 BPM）。
- **不揮発フラッシュセーブ（設定保存）**:
  - **`LT` (Shift) + `PLAY`** を押すと、全4トラックの最新パラメータ設定状態がRP2040のフラッシュメモリ（最終セクター）へ安全に書き込まれます。
  - 書き込み中、ヘッダー右端の極小DISKカプセルの枠線とアイコンが **マゼンタ色にネオン点滅（書き込み中）** し、保存が完了すると即座に **シアン色（さわやかな水色）へ常時発光（保存成功）** に変化して視覚的にフィードバックを返します。画面中央に邪魔なテキストをポップアップさせることなく、Core 1側の演奏に一切のノイズを挟まず安全に書き込みが行われます。

---

## 4. 各種パラメータ＆カスタムアイコン解説

UI最上部には、テキストの代わりに視覚的にわかりやすい **高精細 8x8 カスタムアイコン** が表示されています。

|        アイコン         | パラメータ名                   |    操作範囲     | 音響・音楽的な役割                                                                                                   |
| :---------------------: | :----------------------------- | :-------------: | :------------------------------------------------------------------------------------------------------------------- |
| **`SPD`**<br>(文字表記) | **再生速度分周 (Speed)**       |   `x1`〜`x8`    | トラック個別の再生スピード分周比。`x1`は最速、`x8`はゆったりとした展開を作ります。                                   |
|       **`[  ]`**        | **ステップ長 (Length)**        |   `1` 〜 `32`   | シーケンスループの長さ。変拍子（例: `15`, `7`）にしてポリリズムを作ることができます。                                |
|         **`⁙`**         | **トリガー密度 (Density)**     | `0` 〜 `Length` | ループ内で発音するユークリッドトリガーの数。`A` + `B` 同時押しで `Length` の半分に設定できます。                     |
|         **`➔`**         | **ユークリッドシフト (Shift)** |   `0` 〜 `32`   | リズムパターンの開始位置を後方にローテーションさせ、他のトラックとの噛み合わせをずらします。                         |
|        **`🧬`**         | **突然変異率 (Mutation)**      | `0%` 〜 `100%`  | 25%刻みの設定。`A`/`B`で `0, 25, 50, 75, 100` に移動し、`A` + `B` で即座に `0` に固定できます。                      |
|        **`〰`**         | **ジッター揺らぎ (Jitter)**    | `0%` 〜 `100%`  | マイクロタイミングの「ヨレ・遅れ」を付与します。スネアのゴーストノートや揺れるハイハットに最適です。                 |
|         **`⎴`**         | **ゲート音長 (Gate)**          |  `10` 〜 `100`  | 音の長さ（音価）の比率。`10`付近で極小の**スタッカート**、`90`以上で音が繋がる**レガートスライド**になります。       |
|        **`👑`**         | **ルート音 (Root Note)**       |  `0` 〜 `127`   | 生成されるピッチの音楽的アンカー（基準音）をMIDIノート番号で設定します。                                             |
|        **`🪜`**         | **量子化スケール (Scale)**     |  `S1` 〜 `S8`   | ピッチを特定の音楽的旋律（1:クロマチック、2:メジャー、3:マイナー、4:ペンタトニック、5:フリジアンなど）に制限します。 |
|        **`🎲`**         | **突然変異ボタン (RN)**        |   (トリガー)    | 押すたびに、トラックのリズムと旋律パラメータを音楽的にランダム生成（突然変異）します。表示は `RN`。                  |

---

## 5. ライブパフォーマンス機能：`RND` と画面グリッチ

本シーケンサーには、テクノライブでの高揚感を演出するために強力な **確率的カオス・ジェネレーター** が組み込まれています。

### 5.1. `RN` (突然変異) の仕組み

最右端の **`🎲` (RN)** セルにカーソルを合わせ、`A` または `B` ボタンを押すと、そのトラックに対して以下の突然変異が瞬時に実行されます：

1.  **実用的なステップ長（`8, 12, 16, 24, 32`）** からランダムに再選択。
2.  ステップ長に合わせて、**適度な密度のトリガーパターン** を自動計算して再生成。
3.  **Turing Machineの内部レジスタを完全シャッフル** し、全く新しいピッチ／アクセントシード値を生成。
4.  Gateを適度にばらつかせ、新しいグルーヴを自動付与。
5.  Jitterは強制的に0（揺らぎなし）になります・

---

## 6. トラブルシューティング

- **Q: 画面が白または黒のままで何も表示されない**
  - **A**: SPI接続ピン（特に SCK(GP18) と MOSI(GP19)）が正しくLCDに接続されているか確認してください。また、バックライト（LEDピン）に正しく電源が供給されているかチェックしてください。
- **Q: MIDIシンセから音が出ない**
  - **A**: MIDI TX（GP0）からの結線および、シンセサイザー側の受信MIDIチャンネル（Track 1はデフォルトCh 1〜Track 4はCh 4）が一致しているか確認してください。
- **Q: 保存ボタン（Shift + PLAY）を押した後にPicoがフリーズする**
  - **A**: Picoのフラッシュメモリへ書き込む際、デュアルコア制御（ロックアウト処理）が作動します。最新ファームウェアでは完璧にロックアウト割り込みを処理しているためフリーズは発生しませんが、念のため最新UF2ファームウェアに更新してください。
