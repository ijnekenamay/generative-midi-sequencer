# Generative Techno MIDI Sequencer

## プロジェクト概要 (Overview)
本プロジェクトは、Raspberry Pi Pico (RP2040) のデュアルコアアーキテクチャを活かした、ハードウェア動作の **4チャンネル・ジェネレーティヴMIDIシーケンサー** です。
ユーロラックの定番モジュール「Turing Machine」にインスパイアされた確率的シフトレジスタ（S&Hエミュレーション）と、Bjorklundアルゴリズムによるユークリッドリズム生成を組み合わせ、偶発的かつ極めて音楽的なテクノ・ミニマル系のシーケンスを自律的に生成します。

本システムは、余った **「Pico Tracker」ハードウェア（3.2インチ ILI9341 液晶画面、9つの Kailh Choc V1 キースイッチ、PCM5102 I2S DAC搭載）** を流用する形でファームウェアが最適化されており、人間工学に基づいた高コントラストのモノクロームUIからリアルタイムに操作可能です。

---

## 🛠 実装済みのコア機能仕様 (Current Implementation Specs)

### 1. 4ch 独立 MIDIシーケンサー & クロック同期
*   **4つの完全独立トラック**: 全トラックが独立したシーケンスエンジン（`Track 1`〜`Track 4`）として動作します。
*   **トラック独立のクロック・ディバイド（倍率設定）**: 
    マスタークロックに対して、各トラックごとに個別の倍率（`x1`, `x2`, `x3`, `x4`, `x6`, `x8`, `/2`, `/3` など）をグリッドから選択・設定可能。ポリリズム構築に極めて強力な威力を発揮します。
*   **ステップ長の個別設定 (LEN)**: 各チャンネルごとにシーケンスの長さを `1`〜`32` ステップの間でリアルタイムに変更可能です。
*   **標準 MIDI クロック送信**: UART0 (GP0 TX) を経由して、31,250 bps の標準シリアル MIDI 信号を出力（MIDI Start / Stop / Clock同期に対応）。

### 2. ジェネレーティヴ・リズム & ピッチ生成エンジン
*   **ユークリッド・リズムジェネレータ (`EuclideanGenerator`)**:
    Bresenhamアルゴリズムを応用した軽量かつリアルタイムに計算可能なユークリッドパターン生成。ステップ数 (Length)、パルス数 (Density)、開始シフト (Shift) の3パラメータで制御します。
*   **確率的シフトレジスタピッチジェネレータ (`NoteGenerator`)**:
    16-bitシフトレジスタ（初期値 `0x9E37`）を内蔵。**Mutation（変異確率）** パラメータにより、レジスタのビットがそのままループするか、ハードウェア乱数（Picoの `get_rand_32()`）によって変異するかを 0〜100% の確率で無段階コントロールします。
*   **高精度スケール・クォンタイザー**:
    生成されたランダムなCV値を、選択したスケールおよびルート音（Root Note）に量子化。以下の5つのスケールを瞬時に切り替え可能です。
    1.  **Chromatic (半音階)**
    2.  **Natural Minor (ナチュラル・マイナー)**
    3.  **Phrygian (フリジアン - ダークなアシッド向け)**
    4.  **Dorian (ドリアン - ディープ・テクノ向け)**
    5.  **Minor Pentatonic (マイナー・ペンタトニック - 定番ブルーノート)**

### 3. 特徴的なグルーヴ生成 (Groove & Microtiming)
*   **チャンネル独立のマイクロタイミング・ジッター (JIT)**:
    チャンネルごとに 0〜100% の範囲でジッターを調整可能。Picoの乱数発生器とマイクロ秒タイマーを利用した非同期スケジュールエンジンにより、発音タイミングをランダムにミリ秒単位で遅延させ、機械的なタイミングを崩した極めて有機的で「揺れる」グルーヴ（スウィング/ヨレ）を生み出します。
*   **確率的・非同期ゲート長制御 (GAT)**:
    各ステップのゲート長を 10〜100% で個別設定可能。BPMと連動してステップ幅を動的にマイクロ秒換算し、さらに最大33%の確率的な長さの揺らぎ（ゆらぎエミュレーション）を与え、ノートオフを非同期にスケジュールします。「タッタータン」「タータータッタッタ」など、スタッカートからレガートまで表情豊かなグルーヴを作り出します。
*   **リアルタイム調節可能なマスターBPM**:
    `40` から `250` の範囲でリアルタイムにテンポ調節可能。Core 1 のシーケンスタイマーがマイクロ秒精度で即座にテンポ変化に追従します。

### 4. ハードウェアの潜在能力を引き出すマルチコア・IPC設計
*   **スピンロック付き共有メモリ (Spinlock IPC)**:
    Core 0 (UI/描画スレッド) と Core 1 (超低レイテンシ・リアルタイム音源/MIDI送信) 間の通信に、RP2040のハードウェア・スピンロック (`spin_lock_t*`) を用いた IPC (Inter-Core Communication) 設計を採用。画面の更新やユーザー入力による設定変更時でも、データ競合 (Data Race) やデータティアリングを一切発生させず、完全に同期した低ジッターな再生環境を保証します。
*   **アナログ・クロック同期出力 (I2S DAC経由)**:
    外部アナログ機器と完全に同期演奏させるため、音声用 I2S DAC (GP26 Data, GP21 BCLK, GP22 LRCK) から、マスタークロックと100%同期したアナログクロック（トリガーパルス）を出力。PIOによるステレオ 16-bit I2S ドライバーと、22.05 kHz バックグラウンドタイマー割り込みによる超低レイテンシ動作。
*   **安全なマルチコア・ロックアウトによるフラッシュセーブ**:
    Pico内蔵の 16MB QSPI フラッシュメモリの最終セクター（`0xFFF000`）へダイレクトに設定データを保存。書き込み実行時は、Core 0 側から標準 Pico SDK Multicore Lockout API (`multicore_lockout_start_blocking` / `end_blocking`) を呼び出して Core 1 を完全に一時停止させ、XIPフラッシュからのコード実行によるCPUハング (`HardFault`) を完璧に防ぐ超堅牢な安全設計を実現しています。

### 5. 人間工学に基づく極細モノクローム・UI
*   **ヘッダーエリア**:
    *   スペース効率を極限まで高めた **「BPM表示」**（リアルタイムに可変）。
    *   拍（Beat）と完全に同期して **白黒がネガポジ反転フラッシュ** する巨大なテンポ値表示（16x24ボールドフォント）。
    *   現在のステータスを示すピクセルアートアイコン（再生 ▶ / 停止 ■ / ディスク 💾 / ノート 🎵）。
*   **8列パラメータグリッド (LEN, DEN, SHF, MUT, JIT, GAT, ROT, SCL)**:
    *   高精細320x240画面を活かし、8つ全てのパラメータを横一列にスタイリッシュに表示。
    *   各チャンネルの発音トリガーヒットに合わせて、クロック倍率テキスト（例: `x4`）が**ネガポジ反転フラッシュ**。
    *   選択中のパラメータは **白背景に黒文字のハイライト表示**（非選択のパラメータはダークグレーの枠線付き）。
    *   **MUTE（消音）時のダークアウト**: `RT` ボタンを押すと即座に該当チャンネルがMuteされ、ラベルが `MUTx` に変わり、行全体がダークグレーに減光（ディセーブル表示）。発音動作もミュートされますが、ステップインジケーター（再生ヘッド）は動き続けるため、同期状態は常に把握できます。

---

## 🎛 物理キーマッピング (9-Key Controls)

9個のキースイッチのみで快適かつ高速な操作を行えるよう、修飾キー（Shift）を活用したショートカットシステムを搭載しています。

| 物理キー名 | Pico GPIO | 単体押しの動作 | `LT` (Shift) 同時押しの動作 |
| :--- | :--- | :--- | :--- |
| **UP** | GP5 | カーソルを上のトラックに移動 | グローバルBPMを **+5** 増やす |
| **DOWN** | GP3 | カーソルを下のトラックに移動 | グローバルBPMを **-5** 減らす |
| **LEFT** | GP2 | カーソルを左のパラメータに移動 | - |
| **RIGHT** | GP4 | カーソルを右のパラメータに移動 | - |
| **A** | GP8 | 選択パラメータ値を **+1** 増やす | 選択パラメータ値を **+10** 高速増やす |
| **B** | GP7 | 選択パラメータ値を **-1** 減らす | 選択パラメータ値を **-10** 高速減らす |
| **RT** (Page) | GP6 | **MUTE / UNMUTE (ミュート切替)** | - |
| **PLAY** | GP10 | シーケンスの一時停止／再生再開 | **フラッシュへ現在設定を保存 (SAVE)** |
| **LT** (Shift) | GP9 | modifier（Shiftキー）としてホールド | - |

---

## 🔌 物理ピンアサイン (Hardware Pinout)

Pimoroni Pico LiPo (または標準 Raspberry Pi Pico) の接続配線マップです。

```text
       [ Pimoroni Pico LiPo ]
         +-----------------+
    TX0  | [GP0]     [VBUS] | ---> DAC 5V 電源 (VIN)
    RX0  | [GP1]     [VSYS] |
   Left  | [GP2]    [3V3_O] | ---> ディスプレイ / MIDI / 共通3.3V
   Down  | [GP3]     [GND]  | ---> 共通 GND
  Right  | [GP4]    [GP28]  |
     Up  | [GP5]    [GP27]  |
     RT  | [GP6]    [GP26]  | ---> DAC I2S DATA
      B  | [GP7]     [RUN]  |
      A  | [GP8]    [GP22]  | ---> DAC I2S LRCK
     LT  | [GP9]    [GP21]  | ---> ---> DAC I2S BCLK
   Play  | [GP10]   [GND]   |
  TFT_CS | [GP11]   [GP20]  |
 TFT_SCK | [GP12]   [GP19]  | ---> TFT_MOSI (SPI0)
TFT_MISO | [GP13]   [GP18]  |
 TFT_DC  | [GP14]   [GP17]  | ---> TFT_RST
         | [GP15]   [GP16]  |
         +-----------------+
```
*(※ 詳細なハードウェア検証および設計注意事項は `PINOUT.md` を参照してください)*

---

## 📂 ディレクトリ構成 (Workspace Layout)

```text
generative-midi-sequencer/
├── CMakeLists.txt              # ルートCMakeビルド設定
├── pico_sdk_import.cmake       # Pico SDKインポート定義
├── README.md                   # 本説明書
├── ARCHITECTURE.md             # ソフトウェア・アーキテクチャ詳細資料
├── PINOUT.md                   # ハードウェア配線・検証資料
├── src/
│   ├── CMakeLists.txt          # ソースビルド・ライブラリリンク設定
│   ├── main.cpp                # エントリーポイント（Core 0/1 の初期化と結合）
│   ├── engine/
│   │   ├── track.hpp / .cpp         # トラック管理（シーケンス進行・MIDIノート制御）
│   │   ├── euclidean.hpp / .cpp     # ユークリッドリズム計算（Bresenhamアルゴリズム）
│   │   ├── note_generator.hpp / .cpp # Turing Machineエミュレーション & スケール量子化
│   │   ├── midi_handler.hpp / .cpp   # UART0 31.25kbpsシリアルMIDI送信制御
│   │   └── storage_manager.hpp / .cpp # 内蔵フラッシュ不揮発性セーブ／ロード
│   └── ui/
│       ├── display_controller.hpp / .cpp # SPI ILI9341 液晶ドライバ (8x8フォント内蔵)
│       ├── input_manager.hpp / .cpp       # 9キーのGPIOデバウンス・長押しスキャナー
│       └── custom_assets.hpp              # 巨大BPMフォント、ピクセルアートアイコン群
```

---

## 🚀 ビルドと動作確認方法 (Build Instructions)

### 前提条件 (Prerequisites)
1. **Raspberry Pi Pico C/C++ SDK** がホストPCにインストールされ、環境変数 `PICO_SDK_PATH` が適切に設定されていること。
2. `arm-none-eabi-gcc` ツールチェーンおよび `CMake` がインストールされていること。

### ビルド実行手順
プロジェクトルートディレクトリにおいて、以下のコマンドを実行します：

```powershell
# 1. ビルド用ディレクトリの作成
mkdir build
cd build

# 2. CMakeプロジェクトの構成
cmake ..

# 3. コンパイルの実行
cmake --build . --config Release
```

### 書き込み手順
ビルドに成功すると、`build/src/` ディレクトリ内に以下のコンパイル成果品が生成されます：
*   **`generative_midi_sequencer.uf2`**: **実機書き込み用バイナリ**

Pico を **BOOTSELモード**（PicoのBOOTSELボタンを押しながらPCとUSB接続）で接続し、PCに認識された仮想ドライブ（`RPI-RP2`）に `generative_midi_sequencer.uf2` ファイルをドラッグ＆ドロップしてください。自動的に書き込まれ、シーケンサーが起動します。
